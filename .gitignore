.gradle/
.tools/
.idea/
.env
.env.*
qwen_key.env
!.env.example
build/
app/build/
local.properties
*.iml
captures/
outputs/qa/
outputs/workbuddy/
.workbuddy-ai/
jdk21/
jdk21.zip
*.jks
*.keystore
/window.xml

# Local presentation build scratch.
submission-assets/.codex-slide-build/
submission-assets/.demo-day-build/

# Local tooling and generated artefacts - not project source.
.claude/
.agents/
.vscode/
graphify-out/
skills-lock.json

# Debug capture artefacts from on-device UI checks. Root-level only, so the
# tracked images (Submission.png, current-ui.png, polished-ui.png) are unaffected.
/clarify_*.png
/polish_*.png
/screen_*.png
/guide_check*.png
/settings_check*.png
/taste_*.png
/wake_check*.png
/ui.xml
/ui_*.xml

# Design-critique snapshots written by the impeccable skill.
.impeccable/
